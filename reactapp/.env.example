# Example environment file for local development
# Set this to your local backend address when running the React app locally.
# If unset, the app falls back to the deployed examly URL.
REACT_APP_API_BASE_URL=http://localhost:8080
